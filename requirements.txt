# Core GUI framework
PySide6>=6.9.0  # Modern Qt-based GUI framework
qtawesome>=1.2.0  # Font Awesome icons for Qt applications

# Core dependencies for functionality
requests>=2.28.0  # For API calls to LLM services (Ollama)

# Development dependencies (optional)
pytest>=7.0.0  # For testing
black>=22.0.0  # For code formatting
flake8>=5.0.0  # For linting

# Packaging (optional)
pyinstaller>=5.0.0  # For creating executables

# Note: All other dependencies are automatically installed as sub-dependencies
# of the packages listed above. No manual management needed.
